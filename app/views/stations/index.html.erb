<div class="alert">
  <h2>Stations list</h2>
</div>
<table class="table">
  <thead>
  <tr>
    <th>Название</th>
    <th>Район</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>


  <% @stations.each do |station| %>

      <tr>
        <td><%= station.name %></td>
        <td><%= station.district_id %></td>
        <td><%= link_to 'Просмотр', station %></td>
        <td><%= link_to 'Удалить', station, method: :delete, data: {confirm: 'Вы уверены?'} %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'New Station', new_station_path %>
